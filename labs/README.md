# Labs

这里保存针对单一机制的短实验。每个 lab 应该有一个清晰问题、最小代码、可重复验证命令和结论。

计划中的实验：

- stdio JSON-RPC 分帧、并发请求与错误处理
- ACP session、cancel 与 permission
- AG-UI 事件词汇和 SSE
- DSH event ↔ AG-UI event 映射
- 同 session 串行与跨 session 并发

教程可以引用 lab，但不复制其实现。完整应用则放在 `projects/`。
